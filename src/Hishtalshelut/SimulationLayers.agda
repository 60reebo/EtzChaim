--------------------------------------------------
-- תיעוד שכבות הסימולציה: Igulim-Yosher
--------------------------------------------------
-- קובץ זה מסביר את כל שכבות המערכת, מהגדרות ועד הרצה.
-- מטרתו להבהיר את הארכיטקטורה ולמנוע טעויות במיקום קוד.

-- 1. Domain/State: הגדרת סוגי נתונים ומבני מצב
--    - IgulimYosherFullState, CircleDesc, YosherDesc וכו'.
--    - אין כאן חוקים או סימולציה, רק מבני נתונים.

-- 2. Rules: חוקי הבנייה והשלבים
--    - פונקציות שמגדירות איך מוסיפים עיגול/יושר, איך נראה שלב היררכי, וכו'.
--    - אין כאן הרצה של Trace שלם, רק טרנספורמציות והגדרות.

-- 3. Engine: מנוע הסימולציה
--    - מריץ את כל שלבי הסימולציה בפועל (Trace), מפעיל את החוקים מה-Rules.
--    - אחראי להפקת Trace, תרגום לשלבים טקסטואליים, וכו'.

-- 4. Runtime/Scenario: תרחישים ודוגמאות
--    - קבצים שמגדירים תרחיש ריצה, ייבוא Engine, והצגת Trace למשתמש/ממשק.

-- דגשים:
-- * אין להריץ סימולציה (Trace) מתוך Rules, רק מתוך Engine/Runtime.
-- * כל חוק בנייה או טרנספורמציה שייך ל-Rules בלבד.
-- * כל קוד הדפסה/הרצה שייך ל-Engine/Runtime בלבד.
-- * סדר זה נשמר בכל המערכת ומונע בלבול ותחזוקה קשה.

-- לעיון נוסף: ראה README בשורש הפרויקט.
